export class __ModelSingular__ {
  public title: string
  public body: string
  public imageURL: string

  constructor (title: string, body: string, imageURL: string) {
    this.title = title
    this.body = body
    this.imageURL = imageURL
  }
}